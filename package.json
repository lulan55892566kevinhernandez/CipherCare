{
  "name": "ciphercare",
  "version": "2.0.0",
  "description": "CipherCare - Privacy-preserving employee benefits platform powered by Zama FHE",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy-v2.js --network sepolia",
    "deploy:local": "hardhat run scripts/deploy-v2.js --network localhost",
    "verify": "hardhat verify",
    "clean": "hardhat clean",
    "check-balance": "hardhat run scripts/check-balance.js --network sepolia",
    "verify-deployment": "hardhat run scripts/verify-deployment.js --network sepolia",
    "update-frontend": "node scripts/update-frontend-config.js",
    "deploy:full": "npm run check-balance && npm run deploy:sepolia && npm run verify-deployment && npm run update-frontend"
  },
  "keywords": [
    "fhe",
    "privacy",
    "benefits",
    "blockchain",
    "zama"
  ],
  "author": "CipherCare Team",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "@fhevm/hardhat-plugin": "^0.1.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@fhevm/solidity": "^0.8.0",
    "fhevm": "^0.5.4",
    "dotenv": "^16.4.0"
  }
}

